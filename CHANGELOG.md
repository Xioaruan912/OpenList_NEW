# 更新日志

本项目的所有重要更改都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增
- 全局代理策略（`/@manage/proxy`）：off / auto（风控时走代理）/ manual 三模式，控制 115 访问侧（列表/搜索/上传）出站代理
  - auto 模式基于 115 风控标记（`IsStorageBlocked`）反应式切换，30s 轮询，正常时直连
  - 新增 `GET/POST /api/admin/proxy/policy`
- 缩略图目录列表展示该目录**所有媒体文件**（非递归），带 `has_thumb` 标记
  - 有缩略图可点击文件名弹窗查看（`GET /api/admin/thumb/view`）
  - 无缩略图标注【无缩略图】；列表受限时提示
- 生成队列控制：暂停 / 恢复 / 清空队列（`/api/admin/thumb/queue/{pause,resume,clear}`）
- 空白/纯色缩略图检测：不再把白图当有效缓存，标记为失败
- 代理抽帧出白图时自动回退直连重新生成

### 修复
- 代理节点真实连通性健康检查：通过节点实测访问/下载/上传，失败节点标记【不可用】并排除
  - 健康结果按节点配置指纹防过期，避免"改了错误 IP 仍显示正常"
  - 连通失败错误映射为中文提示（认证失败/超时/连接被拒等）
- 生成入队只统计真正缺失的缩略图（跳过已有缓存），树"缺 N"与递归生成数一致
- 115 上传初始化改用 `115Browser` UA，修复 v4.0 `files/init` 返回"请升级到最新版本"
- 挂载文件夹选择器重渲染循环（effect 读写 `crumbs`）
- `ss` 代理节点地址映射为 `socks5://`

### 变更
- 移除「一键缩略图」按钮及生成强度/并发配置 UI（固定最大速度）
- 移除「115 网盘受限」提示文案
- 缩略图代理解耦：只管理缩略图下载，115 访问侧由全局代理策略管理

## [0.1.0] - 2026-08-15

### 新增
- 115 扫码登录（多设备/多根挂载/可视化选文件夹）
- 视频缩略图：ffmpeg 抽帧、长视频 3×3 内容拼图、音频封面、图片/目录缩略图
- 缩略图管理后台：目录树、批量生成/排除/清空/重试、挂载迁移、代理选择
- 多出口防风控代理：节点一键部署（gost + trafficd）、流量监控、自动风控切换
- 一键安装脚本 `install.sh`
