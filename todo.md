# TODO

当前未完成/待处理事项（按优先级）。

## 功能已完成（本轮）
- [x] 115 上传初始化改用 `115Browser` UA，修复"请升级到最新版本"（v4.0 files/init 只认 115Browser UA）
- [x] 缩略图上传功能（Thumb 管理页"上传"按钮 + `/api/admin/thumb/upload`）直连模式 9/9 验证通过
- [x] 缩略图生成解锁为最大速度：8 worker、不节流（移除 low/medium/high 强度与批内/批间间隔）
- [x] 移除前端"生成控制/生成强度/生成并发"配置 UI
- [x] 移除前端"115 网盘受限中…"提示
- [x] 115 驱动表单中文化、移除 Proxy 字段（走代理逻辑改为全局上传代理）

## 待处理
- [x] 代理节点真实连通性健康检查：通过节点代理实测 访问目标网站/下载/上传，
      失败节点在 /@manage/proxy 显示【连通失败】并从代理选择中排除
      （当前仅靠 trafficd 探针在线判断，无法发现 407 认证失败等问题）
- [x] 全局代理策略（/@manage/proxy）：off/auto(风控时走代理)/manual，控制 115 访问侧
      出站代理（驱动 API/上传）；auto 反应式基于 IsStorageBlocked，30s 轮询；
      修复 ss→socks5 地址映射；thumb 代理解耦只管缩略图下载
- [ ] 代理修复后，端到端验证 auto 模式下的：
  - 缩略图生成（下载走指派节点）
  - 缩略图上传（上传走上传代理）
- [ ] 增加更多代理节点后，验证 auto 模式下上传/下载代理自动分离（当前仅 1 个可用节点，DL=UL=hk_2）
- [ ] 旧代理节点/无用配置清理（如有）

## 本轮 Thumb 管理页 UI 调整
- [x] 目录树：只有有子目录的文件夹才显示展开箭头
- [x] 目录行"生成/上传"旁增加"删除"按钮（后端新增删除 _thumbnails 文件夹接口）
- [x] 选中目录面板：移除【生成缺失】【重建优化】按钮
- [x] 选中目录面板：在【已有缩略图 x 个】左边增加【共有 i 个媒体】
- [x] 移除【一键缩略图】按钮及其弹窗/功能（runAll/open/startPath）
- [x] 状态栏展示"缓存目录"具体路径

## 最近修复
- [x] 长视频缩略图改 **1x1 单帧中间（50%）**：实测 3 部 >1h 电影（3.7-6.8GB）的 2x2 多帧网格
      深 seek 全部被 115 拒（403/partial）；单帧只 1 次 seek，失败快速回退本地头部抽帧，
      6.8GB 电影 ~20s 稳定出图；移除多帧网格（generateVideoMosaic/buildVideoMosaic）
- [x] 大视频缩略图：`thumb_video_max_size` 0=不限；大文件头部片段自适应加大（3→16→32MB，大 moov 可解析）
- [x] 进度条修复：剩余 = 队列 + 进行中（不再"进行中却显示100%"）；单任务生成时显示不定态旋转
- [x] 暂停/删除队列联动：ffmpeg 接入可取消 context，暂停/清空时杀进行中任务，active_workers/tasks 立即归零，状态标志一致
- [x] 生成入队只统计真正缺失的缩略图（跳过已有缓存），树"缺 N"与递归生成数一致
- [x] 缩略图管理页新增 暂停/恢复/删除队列 功能（worker 暂停门 + 队列清空）
- [x] 空白/纯色缩略图检测：不再把白图当有效缓存，标记为失败（数字诚实）
- [x] 代理抽帧出白图时自动回退直连重新生成（根因：hk_2 代理中继损坏 CDN 视频字节）
- [x] 目录文件列表点击文件名即可弹窗查看该视频缩略图（/admin/thumb/view 读缓存/生成）
- [x] ThumbDir 改为列出目录下所有媒体文件（非递归）：有缩略图可点击查看，
      无缩略图标注【无缩略图】；115 风控列表失败时回退索引并提示"列表受限"

## 备注
- 115 上传 API v4.0：UA 必须为 `Mozilla/5.0 115Browser/<appVer>`，否则返回 403
- 下载仍用 Chrome UA 规避下载接口 WAF（两者互不影响）
