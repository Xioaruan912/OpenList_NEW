# TODO

当前未完成/待处理事项（按优先级）。

## 功能已完成（本轮）
- [x] 115 上传初始化改用 `115Browser` UA，修复"请升级到最新版本"（v4.0 files/init 只认 115Browser UA）
- [x] 缩略图上传功能（Thumb 管理页"上传"按钮 + `/api/admin/thumb/upload`）直连模式 9/9 验证通过
- [x] 缩略图生成并发统一由 `thumb_concurrency` 控制
- [x] 移除前端"生成控制/生成强度/生成并发"配置 UI
- [x] 移除前端"115 网盘受限中…"提示
- [x] 115 驱动表单中文化、移除存储表单 Proxy 字段
- [x] 移除应用内代理节点池、健康探测、GOST 安装、流量统计和自动切换；保留静态 `proxy_address`

## 待处理
- [x] 将缩略图 JSONL 索引迁移到数据库，并使用内容指纹替代路径 MD5
- [x] 用 tache 任务管理器替换手写预热/上传队列
- [x] 增加内部回环 Range Gateway，统一不提供 URL 的 RangeReader 驱动与 ffmpeg 输入

## 缩略图长期运行优化
- [x] Thumb Tree 改为 DB/快照优先返回，完整网盘目录扫描放到后台 reconcile
- [x] 增加本地/远端 orphan 缩略图清理（远端 24h 二次确认 + 分批扫描）
- [x] 将 `.fail` 失败状态迁移到数据库，并按错误类型设置差异化重试时间
- [x] 生成/上传 tache 队列接入 `task_items` 持久化，支持服务重启恢复且清空后不复活
- [x] `/vt` 未命中改为立即返回占位图 + 后台生成/恢复，文件列表不再等待 ffmpeg/Range
- [x] 九宫格候选生成改为异步 job，支持进度轮询与取消
- [x] 上传改为按 storage 独立并发/速率/风控，115 受限不阻塞其它存储
- [x] 增加缓存命中率、生成耗时 P95、Range 来源和失败分类运行指标
- [x] 将生成策略版本纳入内容指纹，为以后输出格式/抽帧算法升级提供可控缓存失效

## 缩略图架构重构 / 用户体验
- [x] 后端增加 `/api/admin/thumb/runtime` 轻量控制面，统一生成/上传/3×3/Tree 实时状态，避免多套高频轮询
- [x] 3×3 候选关闭弹窗/离开页面后继续执行，返回 Thumb 页面可从后台任务中心继续查看结果
- [x] 3×3 支持多任务排队（最多 16 个，单路执行）与显式取消；普通生成繁忙时等待资源，不再要求用户先暂停队列
- [x] 前端拆为 `api.ts` + `types.ts` + `useCandidateController.ts` + 独立 UI components，`Thumb.tsx` 只负责页面编排
- [x] 3×3 任务摘要接口不携带 base64 图片，控制面轮询保持轻量；完整图片仅在查看具体 job 时返回
- [x] 3×3 将 115 单帧 `403/forbidden/pmt` 与全局硬风控拆分：单帧失败继续后续取帧，只有明确 405/429/blocked/服务器开小差才停止整组并标记 blocked
- [x] Tree partial 状态使用 DB 已知计数作为下限，避免远端扫描超时时目录缓存数短暂跳低

## 本轮 Thumb 管理页 UI 调整
- [x] 目录树：只有有子目录的文件夹才显示展开箭头
- [x] 目录行"生成/上传"旁增加"删除"按钮（后端新增删除 _thumbnails 文件夹接口）
- [x] 选中目录面板：移除【生成缺失】【重建优化】按钮
- [x] 选中目录面板：在【已有缩略图 x 个】左边增加【共有 i 个媒体】
- [x] 移除【一键缩略图】按钮及其弹窗/功能（runAll/open/startPath）
- [x] 状态栏展示"缓存目录"具体路径

## 最近修复
- [x] 长视频缩略图=自适应深度单帧（50→30→15→7→3%，取最深可达帧，越接近中间），
      失败回退本地头部抽帧
- [x] 失败原因记录+告警：markThumbFailed 存 msg，状态/目录接口带原因；
      生成新增失败弹出通知（文件名+原因），文件行显示红色"失败"标签（悬停看原因），
      不再静默消失
- [x] 缩略图目录树行对齐：HStack 满宽 + 文件夹名单行省略号截断
- [x] 大文件被 115 拦截（403 pmt）时快速失败并写失败标记（不重试空转）
- [x] 大视频缩略图：`thumb_video_max_size` 0=不限；大文件头部片段自适应加大（3→16→32MB，大 moov 可解析）
- [x] 进度条修复：剩余 = 队列 + 进行中；暂停/删除队列取消进行中生成（ffmpeg 可取消 context）
- [x] 生成入队只统计真正缺失的缩略图（跳过已有缓存），树"缺 N"与递归生成数一致
- [x] 缩略图管理页新增 暂停/恢复/删除队列 功能（tache Pause/Start + manager 原子替换清空）
- [x] 空白/纯色缩略图检测：不再把白图当有效缓存，标记为失败（数字诚实）
- [x] 静态代理抽帧出白图时自动回退直连重新生成
- [x] 目录文件列表点击文件名即可弹窗查看该视频缩略图（/admin/thumb/view 读缓存/生成）
- [x] ThumbDir 改为列出目录下所有媒体文件（非递归）：有缩略图可点击查看，
      无缩略图标注【无缩略图】；115 风控列表失败时回退索引并提示"列表受限"

## 备注
- 115 上传 API v4.0：UA 必须为 `Mozilla/5.0 115Browser/<appVer>`，否则返回 403
- 下载仍用 Chrome UA 规避下载接口 WAF（两者互不影响）
